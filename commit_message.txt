refactor: Extract hooks & Centralize constants/types (Phase 3-5)

- Phase 3: Extract masonry grid logic to `useResponsiveGrid` hook (TDD).
- Phase 4: Centralize constants in `shared/config/constants.ts`.
- Phase 5: Integrate shared types in `shared/types/types.ts`.
- Refactor `SketchEngine`, `FeedList`, `feed.ts` to use shared resources.
- Fix lint strings and build errors.
